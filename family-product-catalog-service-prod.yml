spring:
  datasource:
    url: jdbc:mysql://mysql-container:3306/familykartdb
    username: root
    password: root		

#jpa specific properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

message: in production profile updated