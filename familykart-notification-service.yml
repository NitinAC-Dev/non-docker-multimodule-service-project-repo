spring:
  datasource:
    url: jdbc:mysql://localhost:3307/familykartdb
    username: root
    password: root		

#jpa specific properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: notification-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "org.familykart.com.shared.model"
        spring.json.use.type.headers: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: JavaDevelopmentCourse@gmail.com
    password: hgcx unjc xvjj ymvs
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true  

message: this is Notification service profile